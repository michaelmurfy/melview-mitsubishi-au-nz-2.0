{
  "pluginAlias": "MitsubishiAUNZ",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Enter your Mitsubishi **Wi-Fi Control** app credentials to discover your units.",
  "schema": {
    "type": "object",
    "properties": {
      "user": {
        "title": "User Name",
        "type": "string",
        "required": true,
        "description": "Your user id, e.g. username@domain.com"
      },
      "password": {
        "title": "User Password",
        "type": "string",
        "required": true,
        "description": "Password used on your Mitsubishi Wi-Fi Control app"
      },
      "dry": {
        "title": "Enable Dry Mode (experimental)",
        "type": "boolean",
        "default": false,
        "description": "Adds a Dehumidifier service for units that support dry mode."
      },
      "fanMode": {
        "title": "Enable Fan-Only Mode",
        "type": "boolean",
        "default": false,
        "description": "Adds a Fan service that lets you run the unit in fan-only mode without heating or cooling."
      },
      "airflowH": {
        "title": "Enable Horizontal Airflow Swing",
        "type": "boolean",
        "default": false,
        "description": "Adds a Switch service for horizontal airflow direction on supported models (ON = sweep, OFF = centre). Vertical swing is always enabled automatically on supported models."
      },
      "fanSpeed": {
        "title": "Enable Fan Speed Control (experimental)",
        "type": "boolean",
        "default": false,
        "description": "Exposes a fan speed slider (Auto / Low / Medium / High) on the main AC tile and on the Fan-Only service. Disabled by default to prevent spurious commands on startup."
      },
      "outdoorTemp": {
        "title": "Enable Outdoor Temperature Sensor",
        "type": "boolean",
        "default": false,
        "description": "Adds a Temperature Sensor accessory showing the outdoor unit temperature (when reported by the unit)."
      }
    }
  }
}